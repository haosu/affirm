%header
  = link_to 'Home', goals_path
  = link_to current_user.name, user_path(current_user)
  = link_to 'My Goals', user_path(current_user)
  = link_to 'Log out', destroy_user_session_path, method: :delete
